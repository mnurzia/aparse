
{
    "version":"0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}/build/"
        },
        {
            "name": "(python) Launch active file",
            "type": "python",
            "request": "launch",
            "program": "${fileDirname}/${fileBasename}",
            "args": [],
            "cwd": "${fileDirname}/"
        }
    ]
}
